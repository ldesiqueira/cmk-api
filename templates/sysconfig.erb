# The path where cmk-api is installed
SITE="<%= "/omd/sites/#{@site}/cmk-api" %>"

# The name of the OMD site which runs check_mk
SITE="<%= @site %>"

# The port to listen on
PORT="<%= @port %>"

# The user name to run as
RUN_AS="<%= @site %>"
